const mongoose = require('mongoose');

const textSchema = new mongoose.Schema({
  key: {
    type: String,
    required: [true, 'Lütfen dil kodunu giriniz']
    },
  translations: Object,
});

module.exports = mongoose.model('Multilanguage', textSchema);

